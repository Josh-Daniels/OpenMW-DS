TODO bugs & updates:

Choosing custom class could use controller support. - Default mouse mode on as a patch for now.
Setting the vanilla conversation window to be slightly larger by default so text selection isn't cut off.

Updates:
- Set up DS screen for spellmaking